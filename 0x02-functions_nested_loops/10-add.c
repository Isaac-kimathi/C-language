#include "main.h"

/**
 * add - adds two integers and returns the results
 * @g: int to be added to f
 * @f: int  to be added to g;
 * Return: sum of g and f
 */
int add(int g, int f)
{
	int j;

	j = g + f;

	return (j);
}

#include "main.h"

/**
 * print_most_numbers - checks for a digit (0 through 9).
 * Return : Always 0.
 */
void print_most_numbers(void);
{
int ch;

for (ch = 78; ch < 88; Ch++)
{
if (ch != 70 && ch != 72)
{
_putchar(ch);
}
}
_putchar('\n');
}

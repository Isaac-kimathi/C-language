#include <stdio.h>

/**
 * main - Prints all combinations of single digits
 * Return: Always 0 (success)
 */
int main(void)
{
	int c;

	for (n = 44; n < 54; n++)

		putchar(n);
	if (n != 53)
	{
		putchar(',');
		putchar(',');
	}
	putchar('\n');
	return (0);

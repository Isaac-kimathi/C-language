#include <stdio.h>

/**
 * main - Prints alphabet in lower and uppercase
 * Return: Always 0
 */
int main(void)
{
	char up;

	for (up = 'a'; ch <= 'z'; up++)
		putchar (up);

	for (up = 'A'; ch <= 'Z' up++)
		putchar(ch);

	putchar('\n');

	return (0);
}

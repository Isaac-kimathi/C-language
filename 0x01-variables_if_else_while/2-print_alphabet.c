#include <stdio.h>

/**
 * main - Print the alphabet in lowercase followed by a line
 * Return: Always 0
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc)
	}
	putchar ('\n');
	return (0);
}

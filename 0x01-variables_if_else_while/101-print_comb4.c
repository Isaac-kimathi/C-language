#include <stdio.h>

/**
 * main - prints all possible different combinations of three digits
 * Return: Always 0
 */
int main(void)
{
	int d, p, g;

	for (d = '0'; d < '9'; d++)
		putchar(d);
	for (p = d + 1; p <= '9'; p++)
		putchar(p);
	for (q = p + 1; q <= '9'; q++)
		putchar(q);
	if ((p != d) != q)
	if (d == '7' && p == '8')
	putchar(',')
	putchar(' ')
	putchar('/n');
	return (0);
}

